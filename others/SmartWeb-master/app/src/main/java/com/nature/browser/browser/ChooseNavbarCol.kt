package com.nature.browser.browser

import com.nature.browser.preference.IntEnum

/**
 * The available proxy choices.
 */
enum class ChooseNavbarCol(override val value: Int) : IntEnum {
    NONE(0),
    COLOR(1)
}
